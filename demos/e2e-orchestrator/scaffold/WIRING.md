# Wiring

Keep the node name. Change the callable. Human (or a coding agent) copies the diff.

Use `langchain.agents.create_agent`. Do not emit the deprecated ReAct prebuilt.

- `code_agent`: ABSTAIN — edit `add_node(...)` or `tools=`
- `code_exec`: ABSTAIN — edit `add_node(...)` or `tools=`
- `data_agent`: ABSTAIN — edit `add_node(...)` or `tools=`
- `mcp_arxiv`: ABSTAIN — edit `add_node(...)` or `tools=`
- `mcp_warehouse`: ABSTAIN — edit `add_node(...)` or `tools=`
- `research_agent`: ABSTAIN — edit `add_node(...)` or `tools=`
- `skill_lint`: ABSTAIN — edit `add_node(...)` or `tools=`
- `sql_query`: ABSTAIN — edit `add_node(...)` or `tools=`
- `supervisor_gate`: FlipToDet — edit `add_node(...)` or `tools=`
- `task_router`: ABSTAIN — edit `add_node(...)` or `tools=`
- `web_search`: ABSTAIN — edit `add_node(...)` or `tools=`
