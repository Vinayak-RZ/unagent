# Wiring

Keep the node name. Change the callable. Human (or a coding agent) copies the diff.

Use `langchain.agents.create_agent`. Do not emit the deprecated ReAct prebuilt.

- `apply_patch`: ABSTAIN — edit `add_node(...)` or `tools=`
- `cite_sources`: ABSTAIN — edit `add_node(...)` or `tools=`
- `delivery_orchestrator`: ABSTAIN — edit `add_node(...)` or `tools=`
- `handoff`: ABSTAIN — edit `add_node(...)` or `tools=`
- `implement_agent`: ABSTAIN — edit `add_node(...)` or `tools=`
- `implementer`: ABSTAIN — edit `add_node(...)` or `tools=`
- `lint`: ABSTAIN — edit `add_node(...)` or `tools=`
- `policy_gate`: ABSTAIN — edit `add_node(...)` or `tools=`
- `read_file`: ABSTAIN — edit `add_node(...)` or `tools=`
- `research_agent`: ABSTAIN — edit `add_node(...)` or `tools=`
- `researcher`: ABSTAIN — edit `add_node(...)` or `tools=`
- `retrieve_docs`: ABSTAIN — edit `add_node(...)` or `tools=`
- `review_agent`: ABSTAIN — edit `add_node(...)` or `tools=`
- `reviewer`: ABSTAIN — edit `add_node(...)` or `tools=`
- `run_tests`: ABSTAIN — edit `add_node(...)` or `tools=`
- `security_scan`: ABSTAIN — edit `add_node(...)` or `tools=`
- `supervisor`: ABSTAIN — edit `add_node(...)` or `tools=`
- `web_search`: ABSTAIN — edit `add_node(...)` or `tools=`
