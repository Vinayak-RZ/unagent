---
description: Layer boundaries when creating modules, services, or cross-cutting refactors
alwaysApply: false
globs: "**/api/**,**/services/**,**/app/**,**/src/**,**/packages/**"
---

# Architecture boundaries

| Layer | May call | Must not |
|-------|----------|----------|
| UI / routes | Services, actions, hooks | Direct DB, raw secrets |
| Service | Domain, repositories, external APIs | Render UI |
| Data access | Database, caches | Business policy |
| Domain | Pure logic, types | Framework imports |

Validate at system boundaries. One source of truth per concept. Composition over deep inheritance. New deps need a reason.
