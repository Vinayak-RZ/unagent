---
description: App-repo docs sync — IMPLEMENTATION_PLAN, DECISIONS, PROGRESS after milestones
alwaysApply: false
globs: "**/IMPLEMENTATION_PLAN.md,**/DECISIONS.md,**/PROGRESS.md,**/docs/**/*.md"
---

# Documentation (application repos)

Keep docs in sync with shipped behavior.

When the **target is an application repo** (not this config distro):

- `IMPLEMENTATION_PLAN.md` after plan approval
- `DECISIONS.md` for significant choices
- `PROGRESS.md` during phased work

This config repo’s overview is `README.md`. Do not invent `PROJECT_OVERVIEW.md` here.
Skills stay portable: never require a named customer repo to use them.
